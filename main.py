from moviepy.editor import ColorClip
import random

print("🚀 جاري صناعة الفيديوهات وكتابة السيو...")

# نفتح ملف نصي لنكتب فيه الكلمات المفتاحية
with open("seo_keywords.txt", "w", encoding="utf-8") as f:
    f.write("📄 قائمة العناوين والكلمات المفتاحية لفيديوهاتك\n")
    f.write("=========================================\n\n")

    # حلقة التكرار للـ 20 فيديو
    for i in range(1, 21):
        # 1. صناعة الفيديو
        color = (random.randint(0, 255), random.randint(0, 255), random.randint(0, 255))
        filename = f"video_{i}.mp4"
        clip = ColorClip(size=(1080, 1920), color=color, duration=2)
        clip.write_videofile(filename, fps=24)
        
        # 2. كتابة السيو (العنوان والكلمات) في الملف النصي
        # هنا نكتب عنواناً جذاباً ووصفاً يحتوي على هاشتاجات
        title = f"فيديو ألوان مريح للأعصاب رقم {i} 🎨 #shorts"
        description = f"شاهد هذا الفيديو الملون المذهل! هذا هو الفيديو رقم {i} من سلسلة الألوان.\n\n#colors #satisfying #viral #trending #فيديو_قصير"
        
        # حفظ المعلومات داخل الملف النصي
        f.write(f"--- 🎬 فيديو رقم {i} ---\n")
        f.write(f"📌 العنوان: {title}\n")
        f.write(f"📝 الوصف والكلمات: {description}\n")
        f.write("\n----------------------------------
        
